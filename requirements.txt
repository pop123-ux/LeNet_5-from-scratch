# Core — the model itself only needs torch; torchvision supplies the MNIST
# dataset loader and the Resize/ToTensor transforms.
torch>=2.0
torchvision>=0.15

# test.ipynb — training curve, confusion matrix and classification report
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.2
pandas>=2.0

# Needed to open test.ipynb, which is the entry point for this repo
jupyter>=1.0

# Note: for a CUDA build of torch, install from the official index instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# The plain PyPI wheels above are CPU-only on some platforms.
